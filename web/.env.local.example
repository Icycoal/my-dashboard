# Copy to .env.local and fill in your own values. Never commit .env.local.

# Claude Code subscription auth for the apartment agent (optional).
# Generated via `claude setup-token`. Keep ANTHROPIC_API_KEY unset so it can't override this.
CLAUDE_CODE_OAUTH_TOKEN=

# Optional: override the agent model
# APARTMENT_AGENT_MODEL=
